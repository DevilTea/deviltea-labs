/**
 * Git execution and commit-tree materialization.
 *
 * Public re-export surface for the EF Core git layer: the sanitized
 * process-execution boundary and the typed Git plumbing adapter.
 */

export * from './executor'
export * from './repository'
